# 企業別設定(導入先の意向)

> このファイルは `/setup` の一問一答ヒアリングで自動生成されます。
> 現在は **未設定** です。導入時に Claude Code 上で `/setup` を実行してください。

## 企業情報

- 企業名: (未設定)
- 事業内容: (未設定)
- 導入担当: (未設定)

## 業務内容・ユースケース

(未設定)

## 応答の口調・スタイル

(未設定 — 設定されるまでは共通ルールの応答スタイルに従う)

## 有効化した追加エージェント

(なし — `/setup` でライブラリから選択・有効化される)
